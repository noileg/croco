# 検討中：Obsidianメモ連携（2026-07-30時点、途中経過）

受験資料作成に戻るため、ここで一旦中断。次に開くときはここから再開すること。
**まだ何も実装していない。設計の途中経過のメモ。**

## 発端

`C:\Users\Seita\Downloads\additional_spec.md`（Geminiとの検索用の壁打ちで作った下書き）を
読むところから始まった。内容は「Notion Inbox DBに『ただのメモ』種別を追加し、
AI処理を完全にスキップしてローカルのObsidianへ直接書き出す」という提案。

## この下書きの前提のズレ（確認済み）

- 実際のクロコはPowerShell 1本ではなくPython（`run_croco.py --capture`/`--dispatch`）。
  下書き5章のプロンプト案はそのままでは使えない。
- 「ただのメモ」を新設しなくても、既存の`資料`種別（`croco/inbox.py`の`KIND_MATERIAL`）が
  ほぼ同じ役割（実装対象外・ステータス「対象外」）を持っている。2.6章A-3で確定済み。
- 生ログは種別に関わらず必ずGeminiの分割を通る（要約させない・逐語転記のための一線）。
  「AIを完全スルー」は分割自体ではなく、分割後に実装ストリームへ回すかどうかの話。
- Notionからの即削除は設計原則（失敗したら何もしない・存在自体が状態の印）と衝突する。
  書き出し後も削除ではなくアーカイブ／ステータス変更で倒すべき。

## 対話で分かった、本人の実際のニーズ（下書きとは別物に着地しつつある）

1. 最初は「気兼ねなく書けるメモが欲しい」という話だった。
2. 深掘りすると実態は「Notionが重い・同期に時間がかかる・Notion AIが無いと
   使いにくい」という**速度・UXの問題**で、AIに判断されることへの心理的抵抗ではなかった。
3. さらに深掘り：
   - **Obsidianはスマホから書く**（Obsidianモバイルアプリで直接メモを取る）。
   - **PC側は特別な仕組みが要らない**。Claude Codeがvaultのパスを渡せば直接ファイルを
     読めるので、「AIに読ませる」ための専用APIやプラグインは不要
     （additional_spec.md③の「PCにObsidianを常時並置」という前提も撤回でよさそう）。
   - Dify壁打ちの使いにくさ（調べもの起点の衝動的な壁打ちがGoogleのsearch modeに
     流れてしまう件）も話に出たが、**これは3章「第二段階」のスコープなので今回は解かない**。

## 最終的に見えてきた本題

**スマホのObsidianアプリで書いたメモを、無料でPCまで届ける手段が無い**
（Obsidian公式同期は990円/月、他の無料の同期手段も無い）。
これを埋めるために、**Obsidianプラグインを作る**、というのが今の目的地。

叩き台の案（本人の確認は取れていない）：
プラグインがNotion APIを直接叩いて、書いたメモをNotion側へ投げる。
人間はNotionを二度と開かない（画面には出さない、完全に見えない中継として使うだけ）。
PC側のクロコがそれを拾い、「Obsidian由来の単純メモ」と分かる形（別データソース／
専用フラグ等、未確定）であれば、Gemini分割・分類を経由させずローカルのObsidian
vaultへそのまま`.md`書き出しする。

## 決まったこと（確認済み）

- アイデア/予定の既存フロー（スマホのNotion AIチャット→未処理置き場→クロコ）は
  **変更しない**。将来Difyに移行するまでの繋ぎとして現状維持でよい、と本人が明言。
- PC側で常時Obsidianを開いておく必要はない（Claude Codeで直接読めるため）。

## 未決定（次回、ここから詰める）

1. プラグインの転送先はNotion API経由でいいか（他の無料の飛ばし方があるか）。
2. 書き込みの向きは片方向（スマホ→PC）だけでいいか。PC側でクロコが`資料`と
   判定したものを、同じプラグイン経由でスマホのObsidianへ戻したいか。
3. リポジトリ配置：下書きエディタ（Twitter-like-char-counter）の前例に倣い、
   クロコ本体とは別リポジトリにするか。
4. Notion側での「Obsidian由来メモ」の識別方法（別データソース／専用種別／
   専用フラグ、いずれも未検討）。
5. `資料`種別をローカルのObsidian vaultへ書き出す処理そのもの（前段の議論）：
   書き出しのタイミング（captureの一部か独立フェーズか）、書き出し後のNotion側の
   扱い（残す／アーカイブ／別ステータス）、ファイル名規則
   （`[起草日時]_[タイトル].md`のたたき台はあるが未確定）、`予定`種別を含めるかどうか。

## 参考

- 下書きの原本：`C:\Users\Seita\Downloads\additional_spec.md`（Downloadsは一時置き場。
  必要になったらこちらへコピーするか本人判断）。
- この検討と並行して、無関係の`related.py`（関連アイテム機能）が未コミットのまま
  残っている（2026-07-30時点）。今回の話とは別件なので触っていない。
